def int2bin(n, count=24):
    a= "".join([str((n >> y) & 1) for y in range(count-1, -1, -1)])

    c=0
    while int(a[c])==0:
        c+=1

    return a[c-count:]


def ispalindromic(n):
    n=str(n)
    return cmp(n,n[::-1]) 


sum=0
for num in range (1,1000000):
    if ispalindromic(num)==0 and ispalindromic(int2bin(num))==0:
        sum += num


print sum
